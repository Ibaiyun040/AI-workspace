### AI #29

**值得做，但不要只拿现在的三个权重和 60 分去扫。** 只调 A 的线性配方，盖不住那些「D 能抓、A 抓不到」的币；网格里必须带上 **U∧F 的交互**，否则优化会在「降阈值换召回」里打转，精确率很快掉下来。

## 先说结论

A 的分数是：

`SSRI = 100 × (0.45×结构 + 0.35×燃料软或 + 0.20×杠杆弹性)`

D 的币（BICO 早期那种）结构分接近 0、U/F 刚过线，线性加重燃料也到不了 60。所以：

- **只网格 A 的权重和阈值**：能检验 0.45/0.35/0.20 和 60 稳不稳，顶多再捡几只「差几分」的币，**覆盖不会接近 A∪D 的 36 只**。
- **网格里加入交互项 $I=f(U,F)$**：才是「一个分数、一个阈值」去逼近 A∪D，同时看精确率掉多少。

目标应写成约束优化，而不是「命中率别掉太多」这种软话：

- **硬约束**：精确率 ≥ **90%**（相对现在 A 的 100%、A∪D 的 94%，允许掉约 4–10 个点）
- **主目标**：最大化 **命中的不同代币数**（其次才是 TP episode 数）
- **次约束**：TUT、BICO 在爆发窗前仍要响（龙虾可以继续不进这个起爆分）

## 建议网格什么、不网格什么

**固定（避免过拟合、也避免和上次口径不可比）：**

- OI≥200 万、48h 并 episode、3 天预热、前 3 天大波动记余波  
- 打标：7 日涨 50% / 1h 振幅 25% / 空爆 3%  
- D/C/K 的几何平均先不动（这是「庄」和「真看空」的门）

**值得扫的（大约几十到一百组，缓存上几分钟就能算完）：**

| 块 | 扫什么 | 为什么 |
|---|---|---|
| 阈值 | SSRI 或综合分 52, 55, 58, 60, 62, 65 | 最直接的覆盖–精确率曲线 |
| 权重 | 结构 / 燃料 / L 在单形上几档，如 (0.50,0.30,0.20)、(0.40,0.40,0.20)、(0.35,0.45,0.20) | 看 A 是不是已经接近最优线性组合 |
| **交互（关键）** | $I=\mathrm{clip}(\frac{U-u_0}{u_1})\times\mathrm{clip}(\frac{F-f_0}{f_1})$，$u_0\in\{0.30,0.40,0.50\}$，$f_0\in\{2.5,3,4\}$，权重 $w_I\in\{0,0.15,0.20,0.25\}$ | 这才是把 D 嵌进一个分 |
| 或开关 | $S=\max(\mathrm{SSRI},\;100\cdot\mathbf{1}_{U>u,F>f})$ | 对照：综合分是否只是 A∪D 换皮 |

**不要扫：** 每个事件单独阈值、按币种改 D/C/K 上下限、用 8 月 9 日去选参。

## 怎么避免把 TUT 背下来

数据按时间切开，不要用全样本选参再在全样本上报分：

- **选参段**：2026-02-15 → 2026-06-30（RAVE/STO/LAB/BANK 等已发生）  
- **确认段**：2026-07-01 → 2026-08-13（**含 TUT/BICO/龙虾，选参时当没看见**）

选参只看：确认段之前精确率≥90%，且命中币最多。然后再在 7–8 月上看还能不能提前打到 TUT/BICO、误报多了没有。

这样「覆盖更多代币」指的是选参段里多出来的 POWER/SIREN/EVAA 一类，而不是拿 8 月三币去调参。

## 我对结果的预判

1. **只动权重和 60 分**：最优大概仍在 58–62 附近，多 1–3 只币，精确率略掉；龙虾（窗前约 45.6）仍然进不来。  
2. **加上 $I$ 或 max 开关**：确认段应接近现在的 A∪D（三十多只币、精确率 90%+）。综合分的价值是 **一个数能排序「离熔断还有多远」**，不是再发明第三条魔法规则。  
3. 若 90% 精确率下确认段覆盖仍明显少于 A∪D，说明线性+一层交互不够，应保持 **两个头、一个执行**：`S≥60 或 D`，不要硬揉成一个阈值。

## 计算量

不要每个参数组合都重拉 907 个币。每个币只算一次原始序列 $D,C,K,F,U,L,T$（已在缓存里），网格只做归一化和加权，一百组是分钟级，**不再撞限流**。

如果你点头按这个约束做，下一步就是：导出分量 → 时间切分网格 → 出一张「精确率–命中币数」表和确认段上的胜出配方。


### User #15

```text
请你转变一下思路，我们现在的目标不再是尽力覆盖前面提到的每次事件，而是我们这个指标目前展示出的能力已经具备一定的量化因子价值了，我们现在做的是进行量化因子的发掘，所以需要进行网格扫描，懂了吗？根据这个目标来看，我们应该怎么样进行指标回测优化
```
